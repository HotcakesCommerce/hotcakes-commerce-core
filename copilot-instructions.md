# Copilot Instruction Pointer

The canonical repository instructions are in `AGENTS.md`.

GitHub Copilot repository-wide instructions are in
`.github/copilot-instructions.md`. Read and follow both files before suggesting,
reviewing, or changing code.

This repository is Hotcakes Commerce (HCC-Core), targeting DNN 9.13.0 and .NET
Framework 4.8. Preserve its existing store-scoped services, repositories,
factory methods, WebForms/Razor/Knockout/Bootstrap 4 UI, `hcc_` database
objects, versioned SQL-provider upgrades, packaging workflow, and public
extension points.

Do not publish, push, deploy, open pull requests, or share proprietary content
without explicit authorization.
